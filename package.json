{
  "private": true,
  "workspaces": [
    "fixture",
    "internal/module_graph",
    "npm/*"
  ],
  "scripts": {
    "build": "npm run build:internal && npm run build:cli && npm run build:fixture",
    "build:internal": "npm run build -w internal/module_graph -w npm/remix -w npm/remix-dev",
    "build:cli": "npm run build -w npm/remix-dev-darwin-arm64 -w npm/remix-dev-darwin-x64",
    "build:fixture": "npm run build -w fixture"
  },
  "engines": {
    "node": ">=16"
  }
}
